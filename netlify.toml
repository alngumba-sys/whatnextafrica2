[build]
  command = "npm run build"
  publish = "dist"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"

# Clear cache on each build to avoid stale module resolution
[build.processing]
  skip_processing = false
